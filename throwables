import PageCard from "@/components/cards/PageCard";
import GridTable from "@/components/tables/GridTable";
import { getSuroiImageLink } from "@/lib/util/suroi";
import { Throwables } from "@/vendor/suroi/common/src/definitions/throwables";

export default function ThrowablesPage() {
  return (
    <main className="w-full">
      <div className="prose prose-invert">
        <h1>Throwables</h1>
        <p>
          There are currently {throwables.definitions.length} throwables in
          the game.
        </p>
      </div>
      <GridTable>
        {HealingItems.definitions.map(item => (
          <PageCard
            title={item.name}
            image={getSuroiImageLink(item)}
            url={`/healing/${item.idString}`}
            description={item.idString}
            key={item.idString}
          />
        ))}
      </GridTable>
    </main>
  );
}
